<?xml version="1.0" encoding="utf-8"?>
<Page x:Class="MiraiPalette.WinUI.Views.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:MiraiPalette.WinUI.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:controls="using:CommunityToolkit.WinUI.Controls"
      xmlns:ui="using:CommunityToolkit.WinUI"
      NavigationCacheMode="Required"
      mc:Ignorable="d">
    <Grid Padding="32">
        <Border>
            <TextBlock Text="设置" Style="{ThemeResource TitleTextBlockStyle}"/>
        </Border>
        <ScrollViewer Padding="8">
            <StackPanel Orientation="Vertical" Spacing="8">
                <Rectangle Height="36"/>
                <TextBlock Text="外观和行为"
                           Style="{ThemeResource BodyStrongTextBlockStyle}"/>
                <controls:SettingsCard HeaderIcon="{ui:FontIcon Glyph=&#xE790;}" Header="主题" Description="设置应用的颜色模式">
                    <ComboBox SelectedValue="{x:Bind ViewModel.ThemeMode,Mode=TwoWay}" SelectedValuePath="Tag">
                        <ComboBoxItem Content="跟随系统" Tag="Default"/>
                        <ComboBoxItem Content="浅色模式" Tag="Light"/>
                        <ComboBoxItem Content="深色模式" Tag="Dark"/>
                    </ComboBox>
                </controls:SettingsCard>
                <TextBlock Text="关于"
                           Style="{ThemeResource BodyStrongTextBlockStyle}" />
                <controls:SettingsExpander Header="Mirai Palette"
                                           Description="© 2025 RemeaMiku. All rights reserved">
                    <TextBlock IsTextSelectionEnabled="True" Text="0.3.9-winui" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                </controls:SettingsExpander>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
